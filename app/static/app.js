// static/app.js
// Placeholder global JS to avoid 404 on Render.
// Later you can place shared UI helpers here.

window.VR_APP = window.VR_APP || { version: "1.0.0" };

document.addEventListener("DOMContentLoaded", () => {
  // no-op
});
