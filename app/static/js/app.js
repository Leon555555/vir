// static/app.js
// App-level JS (placeholder). Keeps console clean and avoids 404.
// Later we can put global helpers here.

window.VR_APP = window.VR_APP || {
  version: "v1",
};

document.addEventListener("DOMContentLoaded", () => {
  // no-op
});
