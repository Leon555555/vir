// static/dragdrop.js
// Drag & drop helpers placeholder.
// The coach planner drag-drop can be implemented here later.
// For now it's intentionally minimal to avoid 404 and not break anything.

document.addEventListener("DOMContentLoaded", () => {
  // no-op
});
